<?php
$CONFIG = array(
    "objectstore"   => array(
        "class"     => "\\OC\\Files\\ObjectStore\\S3",
        "arguments" => array(
            "bucket"        => "{{ nextcloud_s3_name }}",
            "autocreate"    => true,
            "key"           => "{{ nextcloud_s3_key }}",
            "secret"        => "{{ nextcloud_s3_secret }}",
            "hostname"      => "{{ nextcloud_s3_domain }}",
            "port"          => 443,
            "use_ssl"       => true,
            {% if nextcloud_s3_region is defined %}
            "region"        => "{{ nextcloud_s3_region }}",
            {% endif %}
            // required for some non Amazon S3 implementations
            "use_path_style" => true
        ),
    ),
);